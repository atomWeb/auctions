AuctionsTableIAM:
  Effect: Allow
  Action:
    - dynamodb:Query
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:PutItem
    - dynamodb:UpdateItem
    - dynamodb:DeleteItem
  Resource:
    - ${self:custom.AuctionsTable.arn}
    - !Join [
        "/",
        ["${self:custom.AuctionsTable.arn}", "index", "statusAndEndDate"],
      ]
